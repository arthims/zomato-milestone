# requirements-streamlit.txt
# Fallback if pyproject.toml editable install doesn't work.
# Usage: pip install -r requirements-streamlit.txt

streamlit>=1.35
datasets>=2.18
httpx>=0.27
python-dotenv>=1.0
rich>=13
typer>=0.12
